#!/usr/bin/env ruby

# Git Sentiment
#
# Track words of interest over time in your git repository. Output is formatted
# as CSV or HTML.
#
# Usage:
#
#   # Track default set of words (shit, fuck, crap)
#   $ git-sentiment
#
#   # Track custom set of words, max of 20 revisions, format as HTML
#   $ git-sentiment --words=love,hate --max-revisions=20 --format=html

require 'git_sentiment'

GitSentiment::CLI.new.execute
